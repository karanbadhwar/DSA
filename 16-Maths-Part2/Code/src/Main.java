public class Main {
    public static void main(String[] args) {
        System.out.println("Maths for DSA Part-2");

        /*
            Properties of Modulo(%)
            1 - (a+b)%m = ((a%m)+(b%m))%m;
            2 - (a-b)%m = ((a%m)-(b%m))%m;
            3 - (a*b)%m = ((a%m)*(b%m))%m;
            4 - (a/b)%m = ((a%m)*(b^-1%m))%m;
            5 - (a%m)%m = a%m;
            6 - m^x % m = 0 for all x E(belonging to) +ve Integers;
         */
    }
}