package Generics;

public class Main {
    public static void main(String[] args) {
        /*
           * Generics -> help us to create a parameterized reference type
             Example - ArrayList<Integer> Integer written in brackets is what generics hold,
                       this means this class will only deal with Integer type variables/values.
           *Generics can only mention Class Types, not primitives; that's why for int we give its wrapper class.
           * Generics even help with Type safety!!
           * Cannot create Instance of Generic Type at Compile time.
           * Cannot create static fields of Type Parameter
         */

//        List<String> stringList = new ArrayList<>();
//        List<Integer> integerList = new ArrayList<>();
//
//        if (stringList.getClass() == integerList.getClass()) {
//            System.out.println("Type information is erased at runtime!");
//        }
//
//        System.out.println(stringList.getClass());
    }


}
